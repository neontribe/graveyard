---
# uid=1000(neonbr) gid=1000(neonbr) groups=1000(neonbr),27(sudo),33(www-data)
- user: name=neon{{ cottage_brand_code }} shell=/bin/bash group=www-data groups=sudo append=yes
  sudo: true
  when: cottage_brand_code is defined

