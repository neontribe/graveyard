pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "git out"
            }
        }
        stage('Test'){
            steps {
                echo "Run functional tests"
            }
        }
    }
}

